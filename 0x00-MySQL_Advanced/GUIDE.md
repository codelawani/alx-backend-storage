Upon success running it

The container now shows up on the docker container section

Now u can attach a shell that lets u directly access it

Then run mysql

The linux container is practically empty :(
